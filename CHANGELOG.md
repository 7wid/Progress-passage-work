# 变更记录

本文件记录可交付版本的新增、变更、修复和已知问题。正式发布时由项目负责人补充版本号、时间和提交标签。

> 文档核对：2026-09-12。下列功能条目描述仓库实现，不代表对应生产环境已启用或完成现场验收。

## [Unreleased]

### 宣传页体验

- 重构公共宣传页的视觉层级，以可读的产品演示、场景选择、深色协作过程与浅黄色行动区替代重复插画和同构卡片。
- 增加四类本地需求示例，支持五步协作、退回调整、验收、重新体验及跨区块场景联动；演示不创建真实需求。
- 增加桌面滚动阶段展示、当前章节导航、移动端点选流程和菜单焦点恢复，保留原生 FAQ 与减少动效支持。
- 注册状态加载时使用中性入口，关闭或查询失败时提供账号获取指引。本次不涉及后端接口、数据库迁移或服务器配置变更。

### 文档维护

- 按实际代码重整需求、工程规范与迭代流程，修正成员认领、草稿删除、权限及账号功能等过期描述。
- 补齐首页、密码恢复、需求方管理路由和文档导航，移除不存在的成员负载/推荐验收清单引用。
- 将源码构建部署移至独立运维文档，统一现有服务器 GHCR 拉取部署入口，移除旧 SSH 搭建教程。
- 将可靠性清单改为可重复回归步骤，移除早期分支、固定测试数量和临时交接说明。

## 当前代码包含的功能（未按正式版本编号归档）

### 已完成

- 新增邮箱验证找回密码、一次性限时重置链接、数据库共享限流与有界异步 SMTP 发信；重置后注销旧会话并发送变更通知。
- 新增忘记密码/设置新密码页面、CSRF 接口测试、凭证重放与数据库并发/回滚测试，以及生产邮件配置和 V8 增量迁移说明。

- 正式开放需求方自助注册配置，注册状态、前后端字段校验、失败恢复和成功回跳形成完整闭环。
- 登录增加账号规范化、连续 5 次失败锁定 15 分钟、成功后清零失败状态和标准会话固定防护；对外仍使用统一认证错误，避免账号枚举。
- 生产 Compose 与环境变量模板增加登录保护参数，并补充已部署服务器开启注册及验收说明。
- 前端质量门禁包含 Prettier 格式检查、TypeScript 类型检查、ESLint、Vitest 和生产构建。
- 后端测试、Docker Compose 配置校验和前端可访问性修复纳入当前基线。

### 发布与运行准备

- 运行、发布交接与恢复要求统一维护于项目流程书和部署说明。
- 提供不输出生产密钥、不启动容器的源码构建发布预检脚本；它不替代现场验收。
- 记录前端 UI 质量约束，要求新功能延续现有设计系统和响应式/可访问性标准。
- GitHub 发布成对应用 digest 和 production 记录；服务器拉取程序具备预检、基线、锁、失败暂停和应用回退保护。
- 提供独立 CD 邮件告警与数据库备份/审计保留程序，默认配置及启用要求见部署文档。

### P1 统计报表导出

- 管理员可以从数据概览页导出当前筛选结果的 CSV 报表。
- 导出内容覆盖核心指标、状态分布、分类分布和新增趋势，并兼容中文表格软件。
- CSV 单元格统一转义并防止公式注入；下载失败提供明确反馈。

### P1 成员负载分析

- 管理员数据概览新增在岗成员负载表，展示当前负载、处理中和待验收数量。
- 负载按主负责人、当前日期范围和分类统一统计，并保留零负载成员供分配时比较。
- 成员负载纳入 CSV 导出，组件补充空态、排序和键盘可达的小屏滚动区域。

### P1 任务成员推荐

- 管理员在需求详情的任务成员区可以查看前三名可解释候选，仍由管理员确认最终负责人。
- 推荐按最新可承接评估的技能标签匹配数、分配后的预计主责任务数和稳定名称顺序排列。
- 推荐卡展示账号、角色、技能匹配、当前负载和预计负载，可一键带入原分配表单但不会自动提交。
- 推荐接口仅向管理员开放，不返回邮箱、手机号等联系信息，也不新增数据库表或迁移。

### P1 需求方待办与首次使用指引

- 需求方首页新增“待我处理”，统一聚合草稿、待补充和待验收事项，并按验收、补充、草稿的业务优先级展示。
- 每项待办明确说明当前含义和下一步，草稿/待补充直达编辑页，待验收直达需求详情的交付验收区域。
- 首页“待我处理”摘要改为真实待办数量，避免把团队处理中事项误报为需求方需要操作。
- 公共首页读取生产注册状态；自助注册关闭时不再提示直接提交，改为账号获取入口和受控开通说明。
- 注册关闭页补充账号获取路径、返回首页/登录操作和密码安全提示。

### P1 需求提交与补充资料可靠性

- 补充资料编辑页显示最新评估的公开补充要求，支持加载失败重试和详情入口，不展示内部备注。
- 修复编辑页保存成功、提交失败后仍携带旧版本号重试的问题；保留已填写内容并说明保存与提交的不同结果。
- 从异步表单校验开始锁定保存/提交操作，写入期间保护离页；版本冲突时停止继续写入并引导核对最新详情。
- 分类加载失败可原地重试，服务端字段错误显示在对应字段旁；编辑目标变化也受未保存提醒保护。
- 增加组件与编辑流程回归测试；网页步骤见[需求提交与补充资料可靠性验收](docs/P1需求提交与补充资料可靠性验收清单.md)。

### 已知事项

- 网页复测、MySQL 增量迁移、SMTP 送达、服务器 CD 启用和备份恢复必须有对应环境记录。
- 邮箱队列尚无持久化重试；需求新建没有服务端幂等键；异机备份不属于当前部署程序实现。
